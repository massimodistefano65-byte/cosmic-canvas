name: 🚀 DEPLOY VELOCE 2026

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 🚀 Deploy to FTP
        uses: sebastianpopp/ftp-action@v2.0.0
        with:
          host: 31.11.35.201
          user: 1235499@aruba.it
          password: ${{ secrets.FTP_PASSWORD }}
          localDir: public
          remoteDir: .
          forceSsl: false
